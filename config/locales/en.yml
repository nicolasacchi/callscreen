# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  hello: "Hello world"
  # Admin console — English mirror of it.yml (the primary locale).
  admin:
    nav:
      dashboard: "Dashboard"
      calls: "Calls"
      recordings: "Recordings"
      contacts: "Contacts"
      phrases: "Phrases"
      tags: "Tags"
      rules: "Rules"
      profile: "Profile"
      costs: "Costs"
      tenants: "Tenants"
      settings: "Settings"
      logout: "Logout"
      logged_in_as: "Logged in as"
      super_admin: "super-admin"
    skip_to_content: "Skip to content"
    dashboard:
      getting_started: "Getting started"
      no_calls_intro: "No calls screened yet. To go live:"
      insights_title: "Screening insights (%{days} days)"
      spam_rate: "Spam rate"
      spam_calls: "Spam calls"
      blocked_before_answer: "Blocked before answer"
      llm_accuracy: "LLM accuracy (reviewed)"
      top_spam_callers: "Top spam callers"
      recent_calls: "Recent Calls"
    calls:
      empty: "No calls yet. Screened calls will appear here once your number starts forwarding to Telnyx."
      empty_filtered: "No calls match this filter."
    contacts:
      empty_html: "No contacts yet. They’re created automatically as people call, or add one with “New Contact”."
      empty_filtered: "No contacts match “%{q}”."
